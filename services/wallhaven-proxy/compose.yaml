name: wallhaven-proxy

services:
  proxy:
    build:
      context: .
      dockerfile: Dockerfile
    image: wallhaven-proxy:latest
    container_name: wallhaven-proxy
    restart: unless-stopped

    environment:
      LISTEN_ADDR: ":8080"
      ALLOWED_ORIGINS: "https://trap.lol,https://www.trap.lol"
      DB_PATH: "/data/wallhaven.db"

    ports:
      - "127.0.0.1:8080:8080"

    volumes:
      - wallhaven-data:/data

    networks:
      - default
      - cloudflare-gateway

    security_opt:
      - no-new-privileges:true

    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 128M
        reservations:
          memory: 32M

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

    labels:
      com.trap-lol.service: "wallhaven-proxy"
      com.trap-lol.description: "Wallhaven API proxy + image proxy + vault storage"

volumes:
  wallhaven-data:
    driver: local

networks:
  cloudflare-gateway:
    external: true
